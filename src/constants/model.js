"use strict";

const MODEL = {
  USER: "User",
};

const COLLECTION = {
  USER: "users",
};

const TIMESTAMPS = {
  createdAt: "created_at",
  updatedAt: "updated_at",
};

module.exports = {
  MODEL,
  COLLECTION,
  TIMESTAMPS,
};
